const TOWER_CONFIGS = {
    SNIPER: {
        cost: 30,
        base_hp: 10,
        shoot_hp_cost: 1,
        shoot_interval_ms: 2000,
        radius: 15,
        color: '#cccccc',
        stroke_color: '#ffffff',
        stroke_width: 2
    }
};

const TOWER_TYPES = {
    SNIPER: 'SNIPER'
};

window.TOWER_CONFIGS = TOWER_CONFIGS;
window.TOWER_TYPES = TOWER_TYPES;